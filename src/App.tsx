import React, { FC } from 'react';
import { AppRouter } from './components/AppRouter/AppRouter';

const App: FC = () => {
  return (
    <div className="App">
      <AppRouter />
    </div>
  );
};

export default App;
